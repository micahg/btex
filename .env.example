# Environment variables for BTEX
# Copy this file to .env and customize as needed

# Path where TV shows are stored (destination)
DEST_PATH=/srv

# Path where torrents are downloaded (source)
SRC_PATH=/src

# Path where finished torrents are located (defaults to SRC_PATH/finished)
# FINISHED_PATH=/src/finished

# Cron schedule for running the processor (default: every 5 minutes)
CRON_SCHEDULE=*/5 * * * *

# Email configuration (optional - leave empty to disable email notifications)
SMTP_HOST=smtp.gmail.com
SMTP_USERNAME=your-email@gmail.com
SMTP_PASSWORD=your-app-password
EMAIL_SENDER=your-email@gmail.com
EMAIL_RECIPIENT=recipient@gmail.com

# Optional: Override Python settings
PYTHONUNBUFFERED=1